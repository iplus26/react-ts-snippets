Create React App uses `http-proxy-middleware` when developement. 

This demo shows in which way, we can assign IP address and `host` http header in proxy, to avoid change your host file (e.g /etc/hosts). 